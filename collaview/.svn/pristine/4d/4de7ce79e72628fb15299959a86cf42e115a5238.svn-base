@charset "UTF-8";


input {font-size: 1rem;}
input.inputBox {padding: 8px;}
input.readonly {background-color: #eee;}
input::placeholder {font-size: 1rem; color: #999;}

.container {padding: 88px 16px 76px;}
.inputWrap {display: flex; justify-content: space-between; align-items: center; gap: 8px;}
.content {margin-bottom: 24px;}
.input {margin-bottom: 16px;}
.inputBox {width: 100%; height: 40px;
    border: 1px solid #ddd; border-radius: 5px;
}
.inputTit {margin-bottom: 5px;
    font-weight: 600;
}
.selectListWrap {padding-top: 40px;}
/* span list - S */
.spanTag {display: block;}
.spanTag >div {display: flex; justify-content: space-between; align-items: center; gap: 8px;}
.spanTag .inputBox {margin-bottom: 3px;}
.spanList {width: 100%;
    font-size: 0.8rem; color: #999;
    padding-left: 8px;
}
.spanList >li {position: relative;}
.spanList >li::after {content: "*";
    position: absolute; left: -6px; top: 3px;
}
/* span list - E */
/* check label - S */
.checkWrap {
    min-height: 50px;
    position: relative;
}
.checkLabel {display: flex; justify-content: space-between; align-items: center;
    width: 100%; height: 45px;
    border: 1px solid #ddd; border-radius: 5px;
    padding: 8px;
    background-color: #fff;
    position: absolute; left: 0; top: 0;
}
.checkContent {
    border: 1px solid #ddd; border-radius: 5px;
    padding: 52px 8px 8px;
    background-color: #f7f7f7;
}
.checkText {overflow-y: scroll;
    height: 150px;
    border: 1px solid #ddd; border-radius: 5px;
    padding: 8px;
    font-size: 0.9rem; color: #777; word-break: break-all;
    background-color: #fff;
}
/* check label - E */
/* required - S */
.required .inputTit {width: fit-content;
    position: relative;
}
/* required - E */

.email .inputWrap {display: block;}
.email .inputFlexBox {display: flex; justify-content: space-between; align-items: center; gap: 8px;
    margin-bottom: 8px;
}
.email .inputFlexBox >span {color: #999;}
.email2 {width: 100%;}
.email2 .selectBox {z-index: 4;}
.email2 .selectListWrap {z-index: 3;}
.email2 .selectText {color: #555;}

.phone .inputWrap >span {display: block;
    width: 20px; height: 1px;
    background-color: #999;
}

.birth .inputWrap {gap: 16px;}
.birth .inputWrap >div {display: flex; align-items: center; gap: 3px;}
.birth .select {width: 100%;}
.birth .year {width: 40%;}
.birth .month {width: 30%;}
.birth .day {width: 30%;}

.profile_file {display: flex; justify-content: space-between; align-items: center; gap: 8px;
    margin-bottom: 8px;
}
.profile .inputBox {overflow: hidden;
    display: block;
    width: 100%;
	padding: 10px 8px;
	font-weight: 400; font-size: 0.875rem; line-height: 20px; color: #555555;
	text-overflow: ellipsis; white-space: nowrap;
    background-color: #eee;
}

.privacy {margin-bottom: 8px;}

.btnBox {display: flex; justify-content: space-between; gap: 16px;}

.user-info{
	display: flex;
	margin-bottom: 17px;
}

.avatar {
	overflow: hidden;
	display: block;
	width: 40px;
	height: 40px;
	margin-right: 10px;
	border-radius: 50%;
	background: var(--noneChannelImage) no-repeat center/cover;
}

.user-info-nick{
	font-size: 16px;
}

.user-info-attitude{
	font-size: 12px;
	color: #777;
}
